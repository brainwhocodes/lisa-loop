# Codex Configuration

# Backend Selection (cli | sdk)
# cli: Uses `codex exec` command line interface (default)
# sdk: Uses @openai/codex-sdk Node.js package
backend=cli

# CLI Configuration (used when backend=cli)
codex_exec_timeout=600
codex_resume_session=true

# SDK Configuration (used when backend=sdk)
sdk_runner_path=../src/codex_runner.js
sdk_output_file=.codex_sdk_output.txt

# Loop Context
loop_context_enabled=true
loop_context_lines=5

# File Operations
skip_git_repo_check=false
